<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DefaultMarginTheme">
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginEnd">16dp</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

    <style name="NoMarginTheme">
        <item name="android:layout_marginStart">0dp</item>
        <item name="android:layout_marginEnd">0dp</item>
        <item name="android:layout_marginTop">0dp</item>
        <item name="android:layout_marginBottom">0dp</item>
    </style>

    <style name="RecyclerViewStyle" parent="NoMarginTheme">
        <item name="android:paddingBottom">8dp</item>
        <item name="android:clipToPadding">false</item>
        <item name="android:scrollbars">vertical</item>
    </style>

    <style name="CardViewMarginStyle" parent="DefaultMarginTheme">
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">0dp</item>
    </style>

    <style name="TextPreviewMarginStyle" parent="DefaultMarginTheme">
        <item name="android:layout_marginTop">8dp</item>
    </style>

    <style name="NoteContentMarginStyle" parent="DefaultMarginTheme">
        <item name="android:layout_marginBottom">0dp</item>
    </style>

    <style name="NoteContentFragmentTheme" parent="DefaultMarginTheme">
        <item name="android:orientation">vertical</item>
    </style>

</resources>
